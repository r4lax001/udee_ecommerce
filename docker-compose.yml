services:
  db:
    image: mysql:8.0
    container_name: udee_db
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: udee
    ports:
      - "3307:3306"
    volumes:
      - udee_db_data:/var/lib/mysql

  backend:
    build: ./backend
    container_name: udee_backend
    restart: always
    working_dir: /app
    ports:
      - "5001:5000"
    environment:
      DATABASE_URL: "mysql://root:password@db:3306/udee"
      JWT_SECRET: "udee_secret_key_2024"
      PORT: 5000
    depends_on:
      - db
    volumes:
      - ./backend:/app
      - /app/node_modules
    command: npm run dev

  frontend:
    build: ./frontend
    container_name: udee_frontend
    restart: always
    working_dir: /app
    ports:
      - "5173:5173"
    depends_on:
      - backend
    volumes:
      - ./frontend:/app
      - /app/node_modules
    command: npm run dev -- --host 0.0.0.0

volumes:
  udee_db_data: